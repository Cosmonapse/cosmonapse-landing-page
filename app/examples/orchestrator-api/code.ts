// GENERATED by scripts/sync-examples.mjs from cosmonapse-examples/05-orchestrator-api.
// Do not edit - change the example and re-run the script.

export const CODE: Record<string, string> = {
  "brain.py": "\"\"\"Orchestrator-API brain - one worker, one HTTP Receptor.\n\n    python brain.py\n    curl -s localhost:8000/ask -H 'content-type: application/json' -d '{\"prompt\": \"hi\"}'\n\nAlready running Flask or a raw WSGI server? flask_app.py and wsgi_app.py\nkeep that framework at the edge and dispatch from an orchestrator node of\ntheir own (build_edge below). Run them against a shared synapse:\n\n    cosmo synapse start memory --namespace=api-demo\n    SYNAPSE_URL=cosmo://127.0.0.1:7070 python brain.py     # the worker (+ /ask)\n    SYNAPSE_URL=cosmo://127.0.0.1:7070 python flask_app.py  # :5000\n\"\"\"\nimport asyncio\nimport contextlib\nimport signal\n\nfrom cosmonapse import (Dendrite, MemoryRegistryStore, MemorySynapse,\n                        connect_synapse, run_brain)\n\nfrom config import NAMESPACE, SYNAPSE_URL\nfrom neurons import chat\nfrom receptors import api\n\n\ndef build_worker(synapse) -> Dendrite:\n    node = Dendrite(synapse=synapse, namespace=NAMESPACE,\n                    dendrite_id=\"worker-node\", role=\"worker\")\n    node.attach_axon(chat.AXON)\n    return node\n\n\ndef build_api(synapse) -> Dendrite:\n    \"\"\"registry_store powers find_neurons() for the /neurons route.\"\"\"\n    node = Dendrite(synapse=synapse, namespace=NAMESPACE,\n                    dendrite_id=\"api-node\", heartbeat_s=0,\n                    registry_store=MemoryRegistryStore())\n    node.attach_receptor(api.RECEPTOR)\n    return node\n\n\ndef build_edge(synapse, dendrite_id: str) -> Dendrite:\n    \"\"\"An orchestrator node for a framework you already run (Flask, WSGI).\"\"\"\n    return Dendrite(synapse=synapse, namespace=NAMESPACE,\n                    dendrite_id=dendrite_id, heartbeat_s=0)\n\n\ndef _stop_on_signals() -> None:\n    \"\"\"Ctrl-C / SIGTERM cancel the task so run_brain can unwind every node.\"\"\"\n    task = asyncio.current_task()\n    loop = asyncio.get_running_loop()\n    for sig in (signal.SIGINT, signal.SIGTERM):\n        with contextlib.suppress(NotImplementedError, AttributeError):\n            loop.add_signal_handler(sig, task.cancel)\n\n\nasync def main() -> int:\n    _stop_on_signals()\n    if SYNAPSE_URL:\n        synapse = await connect_synapse(SYNAPSE_URL)\n    else:\n        synapse = MemorySynapse()      # in-process bus - no broker, no setup\n        await synapse.connect()\n    try:\n        return await run_brain(\n            build_worker(synapse),\n            build_api(synapse),\n        )\n    except asyncio.CancelledError:\n        return 0\n    finally:\n        await synapse.close()\n\n\nif __name__ == \"__main__\":\n    try:\n        raise SystemExit(asyncio.run(main()))\n    except KeyboardInterrupt:\n        raise SystemExit(130)\n",
  "config.py": "\"\"\"Shared settings for the orchestrator-api package.\"\"\"\nimport os\nfrom pathlib import Path\n\nfrom dotenv import load_dotenv\n\nROOT = Path(__file__).resolve().parent\nload_dotenv(ROOT.parent / \".env\")               # cosmonapse-examples/.env\n\n# Unset -> brain.py runs on an in-process MemorySynapse (no broker, no\n# setup). Set it (e.g. cosmo://127.0.0.1:7070) to run over a real synapse.\nSYNAPSE_URL = os.environ.get(\"SYNAPSE_URL\", \"\")\nNAMESPACE = \"api-demo\"\n\n\ndef hf_token() -> str:\n    token = os.environ.get(\"HF_TOKEN\", \"\")\n    if not token:\n        raise RuntimeError(\"Set HF_TOKEN in cosmonapse-examples/.env\")\n    return token\n",
  "flask_app.py": "\"\"\"Flask edge - for an app that is already Flask. Sync framework, so the\norchestrator node runs on an asyncio loop in a background thread.\n\n    pip install flask\n    SYNAPSE_URL=cosmo://127.0.0.1:7070 python brain.py        # terminal 2\n    SYNAPSE_URL=cosmo://127.0.0.1:7070 python flask_app.py    # terminal 3\n\n    curl -s localhost:5000/ask -H 'content-type: application/json' -d '{\"prompt\": \"hi\"}'\n\"\"\"\nimport asyncio\nimport threading\n\nfrom flask import Flask, jsonify, request\n\nfrom cosmonapse import connect_synapse\n\nfrom brain import build_edge\nfrom config import SYNAPSE_URL\n\nif not SYNAPSE_URL:\n    raise SystemExit(\"flask_app.py dispatches to brain.py over a shared synapse - set SYNAPSE_URL\")\n\n_loop = asyncio.new_event_loop()\n_edge = None\n_ready = threading.Event()\n\n\nasync def _connect() -> None:\n    global _edge\n    synapse = await connect_synapse(SYNAPSE_URL)\n    _edge = build_edge(synapse, \"flask-edge\")\n    await _edge.start()\n\n\ndef _run_loop() -> None:\n    asyncio.set_event_loop(_loop)\n    _loop.run_until_complete(_connect())\n    _ready.set()\n    _loop.run_forever()\n\n\nthreading.Thread(target=_run_loop, daemon=True).start()\n_ready.wait()\n\napp = Flask(__name__)\n\n\n@app.post(\"/ask\")\ndef ask():\n    prompt = (request.get_json(silent=True) or {}).get(\"prompt\", \"\")\n    if not prompt:\n        return jsonify({\"error\": \"prompt required\"}), 400\n    future = asyncio.run_coroutine_threadsafe(\n        _edge.dispatch_and_wait(neuron=\"worker\", input={\"prompt\": prompt},\n                                timeout_s=30.0),\n        _loop,\n    )\n    try:\n        reply = future.result(timeout=32)\n    except TimeoutError:\n        return jsonify({\"error\": \"worker timed out\"}), 504\n    return jsonify({\"response\": reply.payload[\"output\"][\"response\"]})\n\n\nif __name__ == \"__main__\":\n    app.run(port=5000)\n",
  "handlers_reference.py": "\"\"\"Reference: orchestrator-side Signal handlers (not a runnable app).\n\nAttach these to an orchestrator Dendrite after connecting. They fire for\nevery matching Signal in the namespace - logging, metrics, clarification\nhandling, live progress feeds.\n\"\"\"\nfrom cosmonapse import Dendrite, new_trace_id\n\n\ndef attach_handlers(orchestrator: Dendrite) -> None:\n    # -- AGENT_OUTPUT --------------------------------------------------\n    # Fires when any worker finishes a TASK. dispatch_and_wait already\n    # resolves the caller on the same Signal; use decorators for\n    # side-effects (logging, metrics, webhooks).\n    @orchestrator.on_agent_output\n    async def _log_output(sig):\n        print(f\"[{sig.trace_id[:8]}] output from {sig.directed.id if sig.directed else '?'}\")\n\n    @orchestrator.on_agent_output(neuron=\"worker\")  # narrow by id\n    async def _worker_done(sig): ...\n\n    @orchestrator.on_agent_output(capability=\"text-generation\")  # by capability\n    async def _gen_done(sig): ...\n\n    # -- CLARIFICATION -------------------------------------------------\n    # The Neuron asks a follow-up question. respond_to_clarification\n    # re-dispatches a TASK with the answer attached, same trace_id.\n    @orchestrator.on_clarification\n    async def _handle_clarification(sig):\n        print(f\"[{sig.trace_id[:8]}] clarification: {sig.payload.get('question', '')}\")\n        await orchestrator.respond_to_clarification(\n            sig, answer=\"Please use plain English, no technical jargon.\",\n        )\n\n    # -- ERROR -----------------------------------------------------------\n    @orchestrator.on_error_signal\n    async def _handle_error(sig):\n        print(f\"[{sig.trace_id[:8]}] error: {sig.payload.get('message')}\")\n\n    # -- ESCALATION ------------------------------------------------------\n    @orchestrator.on_escalation\n    async def _handle_escalation(sig):\n        print(f\"[{sig.trace_id[:8]}] escalated: {sig.payload.get('reason')}\")\n        await orchestrator.respond_to_escalation(\n            sig, input={\"override\": \"proceed with best effort\"},\n        )\n\n    # -- THOUGHT_DELTA - streamed reasoning tokens -------------------------\n    @orchestrator.on_thought_delta\n    async def _stream_thought(sig):\n        print(sig.payload.get(\"delta\", \"\"), end=\"\", flush=True)\n\n    # -- TOOL_CALL / TOOL_RESULT - audit logs. Effector servicing does NOT\n    # consume these signals, so trace observers like this still fire. ------\n    @orchestrator.on_tool_call\n    async def _on_tool_call(sig):\n        print(f\"  tool -> {sig.payload.get('tool')}\")\n\n    @orchestrator.on_tool_result\n    async def _on_tool_result(sig):\n        print(f\"  tool <- {sig.payload.get('tool')}\")\n\n    # -- REGISTER / HEARTBEAT - live worker roster -------------------------\n    @orchestrator.on_register_signal\n    async def _on_register(sig):\n        print(f\"worker joined: {sig.directed.id if sig.directed else '?'}  caps={sig.payload.get('capabilities')}\")\n\n    @orchestrator.on_heartbeat_signal\n    async def _on_heartbeat(sig):\n        print(f\"heartbeat: {sig.directed.id if sig.directed else '?'}\")\n\n\nasync def trace_example(orchestrator: Dendrite, prompt: str):\n    \"\"\"on_trace - every Signal type for one workflow in one handler.\"\"\"\n    trace_id = new_trace_id()\n\n    @orchestrator.on_trace(trace_id)\n    async def _trace_all(sig):\n        print(f\"  [{sig.type.value:20}] {sig.directed.id if sig.directed else '?'}\")\n\n    return await orchestrator.dispatch_and_wait(\n        neuron=\"worker\", input={\"prompt\": prompt}, trace_id=trace_id,\n    )\n",
  "neurons/chat.py": "\"\"\"chat - the HF worker every framework edge dispatches to.\"\"\"\nfrom cosmonapse import Axon, Neuron\n\nfrom config import hf_token\n\nAXON = Axon(\n    neuron_id=\"worker\",\n    neuron_fn=Neuron(\n        source=\"huggingface\",\n        endpoint=\"https://router.huggingface.co\",\n        model=\"meta-llama/Llama-3.1-8B-Instruct\",\n        api_key=hf_token(),\n        use_chat_api=True,\n        max_new_tokens=256,\n    ),\n    capabilities=[\"text-generation\", \"chat\"],\n)\n",
  "receptors/api.py": "\"\"\"api - the HTTP edge as a Receptor. No lifespan, no body model, no 504.\n\n    curl -s localhost:8000/ask -H 'content-type: application/json' \\\n         -d '{\"prompt\": \"What is a synapse?\"}'\n\n    # the same endpoint, the other two shapes\n    curl -s  localhost:8000/ask -d '{\"input\": \"...\", \"mode\": \"send\"}'   -H 'content-type: application/json'\n    curl -sN localhost:8000/ask -d '{\"input\": \"...\", \"mode\": \"stream\"}' -H 'content-type: application/json'\n\nA timeout maps to 504 and an ERROR Signal to 500 inside the Receptor. To\nmount it on a FastAPI app you already run: existing_app.include_router(RECEPTOR.router)\nNeeds FastAPI + uvicorn: pip install 'cosmonapse[receptor]'\n\"\"\"\nfrom cosmonapse import ApiReceptor\n\nRECEPTOR = ApiReceptor(\n    neuron=\"worker\",\n    path=\"/ask\",\n    timeout_s=30.0,\n)\n\n\n@RECEPTOR.on_result\ndef render(sig):\n    \"\"\"Terminal Signal -> response body. ERROR is re-raised as a 500.\"\"\"\n    if sig.type.value == \"ERROR\":\n        raise RuntimeError(sig.payload.get(\"message\", \"worker failed\"))\n    return {\"response\": sig.payload[\"output\"][\"response\"],\n            \"trace_id\": sig.trace_id}\n\n\n@RECEPTOR.route(\"/neurons\")\nasync def neurons():\n    \"\"\"An ordinary GET route on the same router.\"\"\"\n    found = await RECEPTOR.dendrite.find_neurons()\n    return {\"neurons\": [n.neuron_id for n in found]}\n",
  "wsgi_app.py": "\"\"\"Raw WSGI edge - no framework; wsgiref ships with Python.\n\n    SYNAPSE_URL=cosmo://127.0.0.1:7070 python brain.py      # terminal 2\n    SYNAPSE_URL=cosmo://127.0.0.1:7070 python wsgi_app.py   # terminal 3, :5001\n    # or: gunicorn --workers=1 \"wsgi_app:application\"\n\"\"\"\nimport asyncio\nimport json\nimport threading\n\nfrom cosmonapse import connect_synapse\n\nfrom brain import build_edge\nfrom config import SYNAPSE_URL\n\nif not SYNAPSE_URL:\n    raise SystemExit(\"wsgi_app.py dispatches to brain.py over a shared synapse - set SYNAPSE_URL\")\n\n_loop = asyncio.new_event_loop()\n_edge = None\n_ready = threading.Event()\n\n\nasync def _connect() -> None:\n    global _edge\n    synapse = await connect_synapse(SYNAPSE_URL)\n    _edge = build_edge(synapse, \"wsgi-edge\")\n    await _edge.start()\n\n\ndef _run_loop() -> None:\n    asyncio.set_event_loop(_loop)\n    _loop.run_until_complete(_connect())\n    _ready.set()\n    _loop.run_forever()\n\n\nthreading.Thread(target=_run_loop, daemon=True).start()\n_ready.wait()\n\n\ndef application(environ, start_response):\n    if environ.get(\"PATH_INFO\") != \"/ask\":\n        start_response(\"404 Not Found\", [(\"Content-Type\", \"application/json\")])\n        return [b'{\"error\":\"not found\"}']\n    length = int(environ.get(\"CONTENT_LENGTH\") or 0)\n    payload = json.loads(environ[\"wsgi.input\"].read(length) or b\"{}\")\n    future = asyncio.run_coroutine_threadsafe(\n        _edge.dispatch_and_wait(neuron=\"worker\",\n                                input={\"prompt\": payload.get(\"prompt\", \"\")},\n                                timeout_s=30.0),\n        _loop,\n    )\n    reply = future.result(timeout=32)\n    body = json.dumps({\"response\": reply.payload[\"output\"][\"response\"]}).encode()\n    start_response(\"200 OK\", [(\"Content-Type\", \"application/json\"),\n                              (\"Content-Length\", str(len(body)))])\n    return [body]\n\n\nif __name__ == \"__main__\":\n    from wsgiref.simple_server import make_server\n    with make_server(\"127.0.0.1\", 5001, application) as srv:\n        srv.serve_forever()\n"
};
