// GENERATED by scripts/sync-examples.mjs from cosmonapse-examples/17-receptors.
// Do not edit - change the example and re-run the script.

export const CODE: Record<string, string> = {
  "brain.py": "\"\"\"Receptors brain - one Neuron, three interfaces. `python brain.py` runs it.\n\n    python brain.py \"what is a synapse\"     one-shot from the terminal\n    python brain.py                         REPL + http://127.0.0.1:8000 (chat)\n                                                  + POST :8000/run   (api)\n\nNothing in neurons/ mentions a CLI, an HTTP server or a chat window. Each\nReceptor turns its edge into the same TASK, so the wire traffic is identical\nwhichever one asked: one TASK out, one AGENT_OUTPUT back, with meta.receptor\nsaying which edge it came through.\n\nThe api and chat Receptors share (127.0.0.1, 8000), so run_brain serves\nthem from one app. Give one a different port= to split them.\n\"\"\"\nimport asyncio\nimport contextlib\nimport signal\n\nfrom cosmonapse import (Dendrite, MemoryRegistryStore, MemorySynapse,\n                        connect_synapse, run_brain)\n\nfrom config import NAMESPACE, SYNAPSE_URL\nfrom neurons import assistant\nfrom receptors import api, chat, terminal\n\n\ndef build_worker(synapse) -> Dendrite:\n    node = Dendrite(synapse=synapse, namespace=NAMESPACE,\n                    dendrite_id=\"worker\", role=\"worker\")\n    node.attach_axon(assistant.AXON)\n    return node\n\n\ndef build_interface(synapse, name: str, receptor) -> Dendrite:\n    \"\"\"One node per interface. registry_store powers find_neurons() - the\n    terminal's `ping` and the api's /neurons.\"\"\"\n    node = Dendrite(synapse=synapse, namespace=NAMESPACE,\n                    dendrite_id=f\"{name}-node\", heartbeat_s=0,\n                    registry_store=MemoryRegistryStore())\n    node.attach_receptor(receptor)\n    return node\n\n\ndef _stop_on_signals() -> None:\n    \"\"\"Ctrl-C / SIGTERM cancel the task so run_brain can unwind every node.\"\"\"\n    task = asyncio.current_task()\n    loop = asyncio.get_running_loop()\n    for sig in (signal.SIGINT, signal.SIGTERM):\n        with contextlib.suppress(NotImplementedError, AttributeError):\n            loop.add_signal_handler(sig, task.cancel)\n\n\nasync def main() -> int:\n    _stop_on_signals()\n    if SYNAPSE_URL:\n        synapse = await connect_synapse(SYNAPSE_URL)\n    else:\n        synapse = MemorySynapse()      # in-process bus - no broker, no setup\n        await synapse.connect()\n    try:\n        return await run_brain(\n            build_worker(synapse),\n            build_interface(synapse, \"terminal\", terminal.RECEPTOR),\n            build_interface(synapse, \"api\", api.RECEPTOR),\n            build_interface(synapse, \"chat\", chat.RECEPTOR),\n        )\n    except asyncio.CancelledError:\n        return 0\n    finally:\n        await synapse.close()\n\n\nif __name__ == \"__main__\":\n    try:\n        raise SystemExit(asyncio.run(main()))\n    except KeyboardInterrupt:\n        raise SystemExit(130)\n",
  "config.py": "\"\"\"Shared settings for the receptors example.\"\"\"\nimport os\nfrom pathlib import Path\n\nfrom dotenv import load_dotenv\n\nROOT = Path(__file__).resolve().parent\nload_dotenv(ROOT.parent / \".env\")               # cosmonapse-examples/.env\n\nSYNAPSE_URL = os.environ.get(\"SYNAPSE_URL\", \"\")  # empty -> in-process bus\nNAMESPACE = \"receptors\"\nMODEL = os.environ.get(\"MODEL\", \"meta-llama/Llama-3.1-8B-Instruct\")\n\n\ndef hf_token() -> str:\n    token = os.environ.get(\"HF_TOKEN\", \"\")\n    if not token:\n        raise RuntimeError(\"Set HF_TOKEN in cosmonapse-examples/.env\")\n    return token\n",
  "neurons/assistant.py": "\"\"\"assistant - one HF chat Neuron. It knows nothing about interfaces.\n\nThat is the point of this example: the same Axon answers a CLI command, an\nHTTP request, and a chat turn, because a Receptor turns each of those into\nthe same TASK. The Neuron never learns which edge asked.\n\n``history`` (sent by ChatReceptor) is folded into the prompt here so the\nconversation carries; a CLI or API caller simply never sends the key.\n\"\"\"\nfrom cosmonapse import Axon, Neuron\n\nfrom config import MODEL, hf_token\n\n_chat = Neuron(\n    source=\"huggingface\",\n    endpoint=\"https://router.huggingface.co\",\n    model=MODEL,\n    api_key=hf_token(),\n    use_chat_api=True,\n    max_new_tokens=400,\n)\n\n\nasync def assistant(payload: dict, context: dict | None = None) -> dict:\n    \"\"\"{prompt|message, history?} -> {reply}\"\"\"\n    text = payload.get(\"prompt\") or payload.get(\"message\") or \"\"\n    turns = payload.get(\"history\") or []\n    if turns:\n        transcript = \"\\n\".join(f\"{t['role']}: {t['content']}\" for t in turns)\n        text = f\"{transcript}\\nuser: {text}\"\n    out = await _chat({\"prompt\": text}, context)\n    reply = out.get(\"response\") if isinstance(out, dict) else str(out)\n    return {\"reply\": reply}\n\n\nAXON = Axon(\n    neuron_id=\"assistant\",\n    neuron_fn=assistant,\n    capabilities=[\"chat\", \"text-generation\"],\n)\n",
  "receptors/api.py": "\"\"\"api - an ApiReceptor: one endpoint, all three dispatch shapes.\n\n    curl -s  localhost:8000/run -H 'content-type: application/json' -d '{\"input\": \"what is a synapse\"}'\n    curl -s  localhost:8000/run -H 'content-type: application/json' -d '{\"input\": \"...\", \"mode\": \"send\"}'\n    curl -sN localhost:8000/run -H 'content-type: application/json' -d '{\"input\": \"...\", \"mode\": \"stream\"}'\n    curl -sN localhost:8000/run/trc_01J...     # a second screen on someone else's trace\n    curl -s  localhost:8000/neurons\n\nThe lifespan, the body model, the 504-on-timeout and the SSE plumbing are\nthe Receptor's job. Mount it into an app you already have with\nexisting_app.include_router(RECEPTOR.router).\n\"\"\"\nfrom cosmonapse import ApiReceptor\n\nRECEPTOR = ApiReceptor(\n    neuron=\"assistant\",\n    path=\"/run\",\n    input_key=\"prompt\",\n    timeout_s=120.0,\n)\n\n\n@RECEPTOR.on_result\ndef render(sig):\n    return {\"reply\": sig.payload[\"output\"][\"reply\"], \"trace_id\": sig.trace_id}\n\n\n@RECEPTOR.route(\"/neurons\")\nasync def neurons():\n    \"\"\"An ordinary route on the same router.\"\"\"\n    found = await RECEPTOR.dendrite.find_neurons()\n    return {\"neurons\": [n.neuron_id for n in found]}\n",
  "receptors/chat.py": "\"\"\"chat - a ChatReceptor: one turn, one dispatch, with a voice add-on.\n\n    open http://127.0.0.1:8000\n\nThe page streams the turn over SSE and, with voice=True, adds a mic button\nand spoken read-back using the browser's Web Speech API. Voice is entirely\nclient side - no audio dependency here, no audio on the wire.\n\nHistory is kept per browser session and rides along in the TASK input as\n`history`; neurons/assistant.py folds it into the prompt. Set\nhistory_turns=0 for a stateless chat.\n\"\"\"\nfrom cosmonapse import ChatReceptor\n\nRECEPTOR = ChatReceptor(\n    neuron=\"assistant\",\n    title=\"Cosmonapse Assistant\",\n    greeting=\"Ask me anything - or press mic and talk.\",\n    voice=True,\n    history_turns=8,\n    timeout_s=120.0,\n)\n",
  "receptors/terminal.py": "\"\"\"terminal - a CliReceptor: a command becomes a TASK.\n\n    python brain.py \"what is a synapse\"      one-shot   (dispatch_and_wait)\n    python brain.py --stream \"...\"           one-shot   (dispatch_and_subscribe)\n    python brain.py --send \"...\"             one-shot   (dispatch_task)\n    python brain.py ping                     local command, no dispatch\n    python brain.py                          REPL, with the HTTP edges up too\n\nWhat is left in this file is what is specific to this program: which\ncommands exist, and what their TASK input looks like. The argparse tree,\nthe REPL, the timeout handling and the printing are the Receptor's.\n\"\"\"\nimport asyncio\n\nfrom cosmonapse import CliReceptor, SignalType\n\nRECEPTOR = CliReceptor(\n    neuron=\"assistant\",\n    prog=\"assistant\",\n    description=\"Talk to a Cosmonapse Neuron from a terminal.\",\n    timeout_s=120.0,\n)\n\n\n@RECEPTOR.command(help=\"ask the assistant\")\ndef ask(prompt: str, tokens: int = 400):\n    \"\"\"The return value IS the TASK input - that is the whole contract.\"\"\"\n    return {\"prompt\": prompt, \"max_new_tokens\": tokens}\n\n\n@RECEPTOR.command(\"ping\", local=True, help=\"is the worker registered?\")\nasync def ping():\n    \"\"\"local=True: answered here, nothing crosses the wire. REGISTER rides\n    the bus like every other Signal, so poll briefly in a fresh process.\"\"\"\n    for _ in range(20):\n        found = await RECEPTOR.dendrite.find_neurons()\n        if found:\n            return {\"neurons\": [n.neuron_id for n in found]}\n        await asyncio.sleep(0.05)\n    return {\"neurons\": []}\n\n\n@RECEPTOR.on_result\ndef render(sig):\n    return sig.payload[\"output\"][\"reply\"]\n\n\n@RECEPTOR.on_print\ndef show(reply: str):\n    print(f\"\\n{reply}\\n\")\n\n\n@RECEPTOR.on_signal(SignalType.TOOL_CALL, SignalType.PLAN)\nasync def progress(sig):\n    # Observation only - delete this and the answers are identical.\n    print(f\"  . {sig.type.value.lower()}\")\n"
};
