// GENERATED by scripts/sync-examples.mjs from cosmonapse-examples/04-pathway.
// Do not edit - change the example and re-run the script.

export const CODE: Record<string, string> = {
  "brain.py": "\"\"\"Pathway brain - one planner worker, one terminal.\n\n    python brain.py shapes\n    python brain.py --stream \"ship feature X\"\n\"\"\"\nimport asyncio\nimport contextlib\nimport signal\n\nfrom cosmonapse import Dendrite, MemorySynapse, connect_synapse, run_brain\n\nfrom config import NAMESPACE, SYNAPSE_URL\nfrom neurons import planner\nfrom receptors import terminal\n\n\ndef build_planner(synapse) -> Dendrite:\n    node = Dendrite(synapse=synapse, namespace=NAMESPACE,\n                    dendrite_id=\"planner-node\", role=\"worker\")\n    node.attach_axon(planner.AXON)\n    return node\n\n\ndef build_terminal(synapse) -> Dendrite:\n    node = Dendrite(synapse=synapse, namespace=NAMESPACE,\n                    dendrite_id=\"terminal-node\", heartbeat_s=0)\n    node.attach_receptor(terminal.RECEPTOR)\n    return node\n\n\ndef _stop_on_signals() -> None:\n    \"\"\"Ctrl-C / SIGTERM cancel the task so run_brain can unwind every node.\"\"\"\n    task = asyncio.current_task()\n    loop = asyncio.get_running_loop()\n    for sig in (signal.SIGINT, signal.SIGTERM):\n        with contextlib.suppress(NotImplementedError, AttributeError):\n            loop.add_signal_handler(sig, task.cancel)\n\n\nasync def main() -> int:\n    _stop_on_signals()\n    if SYNAPSE_URL:\n        synapse = await connect_synapse(SYNAPSE_URL)\n    else:\n        synapse = MemorySynapse()      # in-process bus - no broker, no setup\n        await synapse.connect()\n    try:\n        return await run_brain(\n            build_planner(synapse),\n            build_terminal(synapse),\n        )\n    except asyncio.CancelledError:\n        return 0\n    finally:\n        await synapse.close()\n\n\nif __name__ == \"__main__\":\n    try:\n        raise SystemExit(asyncio.run(main()))\n    except KeyboardInterrupt:\n        raise SystemExit(130)\n",
  "config.py": "\"\"\"Shared settings for the pathway package.\"\"\"\nimport os\n\n# Unset -> in-process MemorySynapse. Set to run over a real synapse.\nSYNAPSE_URL = os.environ.get(\"SYNAPSE_URL\", \"\")\nNAMESPACE = \"demo\"\n",
  "neurons/planner.py": "\"\"\"planner - a plain async function answering \"plan\" TASKs.\"\"\"\nfrom cosmonapse import Axon\n\n\nasync def planner(input, context):\n    return {\"plan\": [\"step-1\", \"step-2\"], \"goal\": input.get(\"goal\")}\n\n\nAXON = Axon(neuron_id=\"planner\", neuron_fn=planner, capabilities=[\"plan\"])\n",
  "receptors/terminal.py": "\"\"\"terminal - the three Pathway consumption shapes, side by side.\n\n    python brain.py \"ship feature X\"          wait    (the Receptor's default)\n    python brain.py --stream \"ship feature X\" iterate (every Signal as it lands)\n    python brain.py shapes                    all three, written out by hand\n\nThe first two are the Receptor doing it for you. `shapes` does the same\nwork directly against the Dendrite, so you can see the primitive underneath:\ndispatch returns a Pathway, and a Pathway can be awaited, subscribed to, or\niterated.\n\"\"\"\nimport asyncio\n\nfrom cosmonapse import CliReceptor, SignalType\n\nRECEPTOR = CliReceptor(\n    capabilities=[\"plan\"],\n    input_key=\"goal\",\n    prog=\"pathway\",\n    description=\"One Pathway, three ways to consume it.\",\n    timeout_s=5.0,\n)\n\n\n@RECEPTOR.command(help=\"plan a goal (routed by capability)\")\ndef plan(goal: str):\n    return {\"goal\": goal}\n\n\n@RECEPTOR.command(\"shapes\", local=True, help=\"run wait / on / iterate by hand\")\nasync def shapes():\n    orch = RECEPTOR.dendrite\n    lines = []\n\n    # 1. await pw.wait() - block until the first terminal Signal, then close.\n    sig = await orch.dispatch_and_wait(\n        capabilities=[\"plan\"], input={\"goal\": \"ship feature X\"}, timeout_s=5.0,\n    )\n    lines.append(f\"1) wait    -> {sig.payload['output']}\")\n\n    # 2. @pw.on(SignalType.X) - callbacks scoped to THIS trace only.\n    pw = await orch.dispatch_and_subscribe(\n        capabilities=[\"plan\"], input={\"goal\": \"ship feature Y\"},\n    )\n    done = asyncio.Event()\n\n    @pw.on(SignalType.AGENT_OUTPUT)\n    async def on_output(s):\n        lines.append(f\"2) on      -> {s.payload['output']}\")\n        done.set()\n\n    await asyncio.wait_for(done.wait(), timeout=5.0)\n    await pw.close()\n\n    # 3. async for sig in pw - walk every Signal on the trace as it arrives.\n    async with await orch.dispatch(\n        capabilities=[\"plan\"], input={\"goal\": \"ship feature Z\"},\n    ) as pw:\n        async for s in pw:\n            lines.append(f\"3) iterate -> {s.type.value}\")\n            if s.type is SignalType.AGENT_OUTPUT:\n                break\n\n    return \"\\n\".join(lines)\n"
};
