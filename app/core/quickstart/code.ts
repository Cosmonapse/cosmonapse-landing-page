// GENERATED by scripts/sync-examples.mjs from cosmonapse-examples/01-quickstart.
// Do not edit - change the example and re-run the script.

export const CODE: Record<string, string> = {
  "brain.py": "\"\"\"Quickstart brain - one HF-backed Axon behind an HTTP Receptor.\n\n    python brain.py\n    curl -s localhost:8000/task -H 'content-type: application/json' \\\n         -d '{\"prompt\": \"Say hello to Cosmonapse.\"}'\n\nneurons/llama.py is the Axon, receptors/api.py is the interface, and this\nfile is deployment: which node hosts what. Set SYNAPSE_URL to run the same\nbrain over a real synapse and watch it in Prism.\n\"\"\"\nimport asyncio\nimport contextlib\nimport signal\n\nfrom cosmonapse import Dendrite, MemorySynapse, connect_synapse, run_brain\n\nfrom config import NAMESPACE, SYNAPSE_URL\nfrom neurons import llama\nfrom receptors import api\n\n\ndef build_llama(synapse) -> Dendrite:\n    \"\"\"Hosts the llama Axon. role=\"worker\": replies to TASKs, never dispatches.\"\"\"\n    node = Dendrite(synapse=synapse, namespace=NAMESPACE,\n                    dendrite_id=\"llama-node\", role=\"worker\")\n    node.attach_axon(llama.AXON)\n    return node\n\n\ndef build_api(synapse) -> Dendrite:\n    \"\"\"Hosts the HTTP Receptor on the one node allowed to dispatch.\"\"\"\n    node = Dendrite(synapse=synapse, namespace=NAMESPACE,\n                    dendrite_id=\"api-node\", heartbeat_s=0)\n    node.attach_receptor(api.RECEPTOR)\n    return node\n\n\ndef _stop_on_signals() -> None:\n    \"\"\"Ctrl-C / SIGTERM cancel the task so run_brain can unwind every node.\"\"\"\n    task = asyncio.current_task()\n    loop = asyncio.get_running_loop()\n    for sig in (signal.SIGINT, signal.SIGTERM):\n        with contextlib.suppress(NotImplementedError, AttributeError):\n            loop.add_signal_handler(sig, task.cancel)\n\n\nasync def main() -> int:\n    _stop_on_signals()\n    if SYNAPSE_URL:\n        synapse = await connect_synapse(SYNAPSE_URL)\n    else:\n        synapse = MemorySynapse()      # in-process bus - no broker, no setup\n        await synapse.connect()\n    try:\n        return await run_brain(\n            build_llama(synapse),\n            build_api(synapse),\n        )\n    except asyncio.CancelledError:\n        return 0\n    finally:\n        await synapse.close()\n\n\nif __name__ == \"__main__\":\n    try:\n        raise SystemExit(asyncio.run(main()))\n    except KeyboardInterrupt:\n        raise SystemExit(130)\n",
  "config.py": "\"\"\"Shared settings for the quickstart package.\"\"\"\nimport os\nfrom pathlib import Path\n\nfrom dotenv import load_dotenv\n\nROOT = Path(__file__).resolve().parent\nload_dotenv(ROOT.parent / \".env\")               # cosmonapse-examples/.env\n\n# Unset -> brain.py runs on an in-process MemorySynapse (no broker, no\n# setup). Set it (e.g. cosmo://127.0.0.1:7070) to run over a real synapse.\nSYNAPSE_URL = os.environ.get(\"SYNAPSE_URL\", \"\")\nNAMESPACE = \"quickstart\"\n\n\ndef hf_token() -> str:\n    token = os.environ.get(\"HF_TOKEN\", \"\")\n    if not token:\n        raise RuntimeError(\"Set HF_TOKEN in cosmonapse-examples/.env\")\n    return token\n",
  "neurons/llama.py": "\"\"\"llama - the quickstart's single LLM Neuron, wrapped in an Axon.\"\"\"\nfrom cosmonapse import Axon\n\nfrom config import hf_token\n\n# Axon.huggingface() (and .openai(), .anthropic(), .ollama(), .mcp())\n# combines the Neuron factory + Axon wiring in one call.\nAXON = Axon.huggingface(\n    neuron_id=\"llama\",\n    endpoint=\"https://router.huggingface.co\",\n    model=\"meta-llama/Llama-3.1-8B-Instruct\",\n    api_key=hf_token(),\n    use_chat_api=True,\n    capabilities=[\"chat\"],\n)\n",
  "receptors/api.py": "\"\"\"api - an ApiReceptor: an HTTP request becomes a TASK.\n\n    curl -s localhost:8000/task -H 'content-type: application/json' \\\n         -d '{\"prompt\": \"Say hello to Cosmonapse.\"}'\n\nThe body is the TASK input. The same endpoint also takes\n{\"input\": ..., \"mode\": \"send\" | \"wait\" | \"stream\"}: fire-and-forget,\nrequest/reply (the default), or an SSE stream of every Signal on the trace.\nNeeds FastAPI + uvicorn: pip install 'cosmonapse[receptor]'\n\"\"\"\nfrom cosmonapse import ApiReceptor\n\nRECEPTOR = ApiReceptor(\n    neuron=\"llama\",\n    path=\"/task\",\n    timeout_s=30.0,\n    # host=\"127.0.0.1\", port=8000 are the defaults\n)\n"
};
